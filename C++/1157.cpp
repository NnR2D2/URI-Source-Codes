#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fl(n) for (int i = 1; i <= n; i++)
#define flj(n) for (int j = 0; j < n; j++)
int main()
{

  ll a, prime = 0;
  cin >> a;
  fl(a)
  {
    if (a % i == 0)
    {
      cout << i << endl;
    }
  }

  return 0;
}
